/* All JS files will be loaded automatically */

$(document).ready(function() {
	
	// Comment if you don't want to use Pjax
	$(document).pjax('a', 'body', {fragment: '#pjax-response'});
	
	
	// Your code here ...
	
});